#include <stdio.h>
int main()
{
	char a;
	
	for (a='A';a<='Z'; a++)
	printf("the ascii of %c is %d \n", a,a); 
	for (a='a';a<='z'; a++)
	printf("the ascii of %c is %d \n", a,a); 
	return 0;
}
